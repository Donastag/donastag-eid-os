# Copy to .env, fill in real values, never commit .env itself.
# In production, set these via Coolify's secrets manager instead of a file on disk.

POSTGRES_USER=donastag
POSTGRES_PASSWORD=changeme
POSTGRES_DB=donastag

REDIS_PASSWORD=changeme

N8N_HOST=n8n.yourdomain.example
N8N_ENCRYPTION_KEY=changeme_generate_a_real_random_key
